interface:
  display_name: "Visualizer Design"
  short_description: "Create host-aware SVG, HTML, Mermaid, and markdown visuals."
  default_prompt: "Use $visualizer-design to explain this topic with a clear visual artifact."

policy:
  allow_implicit_invocation: true
